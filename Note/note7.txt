Tạo một tài khoản trang web rút gọn link
email: codingwiththinh@gmail.com
mật khẩu:
d3#@f5R86ruspPU
Sau đó vào sql để xem thông tin tài khoản
Dùng thư viện Identity để làm form đăng ký một tài khoản

Microsoft: MAUI
Google: Flutter

generate

domain + code (mã được generate)

right -> end (bootstrap)

ctrl D để nhân đôi code của dòng được quét chọn hoặc dòng nơi con trỏ được check vào

toán tử 3 ngôi
a ? b : c

ánh xạ (ít dùng)
mapping (từ thường dùng)

trong blazor người ta dùng tag để gọi một view

margin trục Oxy

truy cập VPS Linux dùng Bitvise
(có nhiều chức năng hơn Putty)

trong lập trình web có nhiều khái niệm trùng tên

render mã cho từng đường link được nhập vào
chuỗi các ký tự từ A tới Z từ a tới z từ 0 tới 9
sẽ dùng các ký tự được liệt kê trong chuỗi trên để tạo code rút gọn link